import { ModalWindow } from "@/layout/modalWindow/modalWindow";
import { BigPopup } from "@/components/reused/bigPopup/bigPopup";

export const StickersInstructions = () => {
  return (
    <ModalWindow modalName={"StickersInstructions"}>
      <BigPopup />
    </ModalWindow>
  );
};
